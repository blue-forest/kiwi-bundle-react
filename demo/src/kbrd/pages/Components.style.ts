import { KBRD } from ".."

export const ComponentsPageStyle = KBRD.StyleSheet(() => ({
  container: {
    padding: 20,
  },
  title: {
    fontWeight: "bold",
    fontSize: 16,
    marginTop: 50,
    marginBottom: 20,
  },
}))
