import { KBRD } from "../app"

export const ComponentsPageStyle = KBRD.StyleSheet({
  textContainer: {
    backgroundColor: "green",
  },
  text: {
    color: "white",
  },
  container: {
    flexDirection: "row",
  },
} as const)
